## Verdict

ReviseDesign

## Comparison

The artifacts agree on the main implementation meaning despite different decomposition:

- The outline's four phases and the candidate's `CAP-B1` through `CAP-B8` both cover six distinct typed stage contracts, exact ordered authority inputs, immutable artifact reads, catalog-contained dispatch, deterministic replay, finite bounds, pure currentness diagnostics, and registration-only extension.
- Both keep lifecycle mutation, publication, review, gates, Provenance operation, claim/exposure fencing, and aggregate-capacity work outside this Bead.
- Both preserve Questions-to-Implementation registration order and Ticket-first, newest-to-oldest accepted technical authority.

They are not deterministic Structure alternatives because their authority and stop states differ materially:

- The outline presents four unchecked implementation phases and validation work as executable Structure without binding an accepted package, authenticated gate response, confirmed contract-valid promotion result, pinned graph snapshot, exact coverage edges, or the approved Structure policy identity.
- Candidate B binds the supplied local identities and classifies the graph, but correctly stops as `AuthorityBlocked`. The bound synthesis outcome is `Fail` and explicitly forbids a Design gate, promotion, or Structure input. The later gate response and promotion request are marked `simulationOnly`, and their result confirms only that simulated request.
- The supplied package lacks the complete `DesignAcceptanceScope`, required Design and promotion policy identities, complete immutable artifact references, ordered obligations and residual-risk decisions, its own artifact identity, and a declared package hash. Reviewer identity, slot, session, verdict conformance, and independence are also unproved.
- The graph contains no selected residual-risk decision records. Candidate B can preserve the reported R1-R5 proposals as unresolved constraints, but neither artifact may treat them as accepted dispositions or use them to authorize terminal work.
- Candidate B's eight capability boundaries are explicitly not a delivery sequence, while the outline prescribes four ordered phases. That ordering difference cannot be resolved as a Structure projection until valid accepted authority exists.

## Coverage And Route

Candidate B gives candidate coverage for all six requirements, eight decisions, and twenty-one controls, names external owners, preserves informational nodes without creating work, and exercises all four required contract scenarios. This supports the substantive comparison but does not cure the missing authority.

The defect is upstream of Structure. A conforming Design acceptance run must bind one complete exact scope, conforming independent reviews, valid synthesis, a complete immutable package, an authenticated package-bound response, and an authoritatively confirmed non-simulated promotion result and graph snapshot that include all accepted residual-risk dispositions. Until then, Structure remains blocked and neither artifact authorizes implementation.
